# MyCut Edge Functions

This directory will contain Deno Edge Functions for MyCut.

## Planned Functions (Step 2+)

| Function | Purpose |
|----------|---------|
| `generate-look` | AI image generation with provider abstraction |
| `rotate-share-code` | Issue fresh QR share codes |
| `redeem-share-code` | Barber scans → returns look + signed URLs |
| `claim-device` | Bind unclaimed tablet to salon |
| `moderate-upload` | Content moderation for source photos |
| `delete-account` | GDPR account deletion |
| `revenuecat-webhook` | Subscription entitlement sync |
| `cleanup-storage` | Process source_photo_deletion_queue |
