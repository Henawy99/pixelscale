name: mycut_cutspec
description: Pure Dart clipper guard ↔ mm conversion math and cut specification models for MyCut.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ">=3.0.0 <4.0.0"

dev_dependencies:
  test: ^1.24.0
  very_good_analysis: ^6.0.0
